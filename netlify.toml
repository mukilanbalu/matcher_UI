[build.environment]
  # Whitelist intentionally public keys so the security scanner doesn't fail the build
  SECRETS_SCAN_OMIT_KEYS = "VITE_API_ENDPOINT,VITE_AUTH0_AUDIENCE,VITE_AUTH0_CLIENT_ID,VITE_AUTH0_DOMAIN,VITE_CLIENT_NAME,VITE_SUPABASE_ANON_KEY,VITE_SUPABASE_URL"
